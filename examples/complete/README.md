# Complete

This example illustrates a more complete setup, deploying a batch account
together with an application, a pool with fixed scaling, a VM image, a start
task, and a job scheduled on that pool.
