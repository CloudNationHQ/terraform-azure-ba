# Private Endpoint

This example illustrates a batch account reachable over a private endpoint, with
public network access disabled. Supporting virtual network and private dns zone
modules wire up the `batchAccount` subresource.
